<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8">
<title>req/studentForm</title>
</head>
<body>
<h1>req/studentForm</h1>
<form action="studentReg" method="post">
    <table border="">
        <tr>
            <td>학생</td>
            <td>국어점수</td>
            <td>영어점수</td>
            <td>수학점수</td>
            <td>반</td>
        </tr>
        <tr th:each="no : ${#numbers.sequence(0,20)}">
            <td><input type="text" th:name="|std[${no}].name|" th:value="${'학생' + no}" /></td>
            <td><input type="text" th:name="|std[${no}].kscore|" th:value="${(T(java.lang.Math).random() * 100 + 1).intValue()}" /></td>
            <td><input type="text" th:name="|std[${no}].escore|" th:value="${(T(java.lang.Math).random() * 100 + 1).intValue()}" /></td>
            <td><input type="text" th:name="|std[${no}].mscore|" th:value="${(T(java.lang.Math).random() * 100 + 1).intValue()}" /></td>
            <td><input type="text" th:name="|std[${no}].clas|" th:value="${(T(java.lang.Math).random() * 5 + 1).intValue()}" /></td>
        </tr>
        <tr>
            <td colspan="5" align="center">
                <input type="submit" value="정렬" />
            </td>
        </tr>
    </table>
</form>
</body>
</html>
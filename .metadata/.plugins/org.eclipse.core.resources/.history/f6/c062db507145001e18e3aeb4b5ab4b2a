package aaa.service;

import java.util.List;

import org.apache.ibatis.annotations.Mapper;

import aaa.model.BoardDTO;
import aaa.model.BoardTestDTO;

@Mapper
public interface BoardTestMapper {
	
	List<BoardTestDTO> list();
	
	BoardTestDTO detail(int id);
	
	int insseerr(BoardTestDTO dto);
	
	int delettt(BoardTestDTO dto);
	
	int modifffy(BoardTestDTO dto);
	
}
